syntax = "proto3";

package proto;

option go_package = "mitra/internal/proto";

message Config {
  ServerConfig server = 1;
  RepoConfig repo = 2;
}

message ServerConfig {
  string port = 1;
  string grpc_port = 2;
}

message RepoConfig {
  string dir = 1;
}
